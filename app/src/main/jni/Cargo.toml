[package]
name = "odfiz_native"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21.1"
# Library sakti untuk olah gambar kilat
image = { version = "0.24", default-features = false, features = ["jpeg", "png"] }
